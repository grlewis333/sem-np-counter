# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['gui_code.py'],
             pathex=['C:\\Users\\grl31\\Documents\\GitHub\\sem-np-counter'],
             binaries=[],
             datas=[('gui_draft_2.ui', '.'), ('logo.png', '.'), ('initial_pic.png', '.')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='gui_code',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='gui_code')
